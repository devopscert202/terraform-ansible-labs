# 03 — Resources and Data Sources

Resources create or manage objects; data sources read existing values. In the EC2 lab, an AMI data source selects the latest Canonical Ubuntu image and `aws_instance` consumes its ID.

Review every plan before applying. An EC2 instance can incur cost; destroy it when the lab is complete.
