# Terraform Essentials HTML Guide

Open `index.html` directly in a browser. The site is fully self-contained: it uses embedded CSS, no CDN, and the Terraform purple `#7B42BC` theme. Pages cover foundations, workflow, variables, state, and modules, with navigation between them.
