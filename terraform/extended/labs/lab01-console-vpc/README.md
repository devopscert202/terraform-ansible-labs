# Lab 01 — Console VPC

This is a reference-only lab. Create and inspect a non-production VPC in the AWS console, then compare its components with the Terraform configuration built in later labs. No Terraform configuration is intentionally included.
