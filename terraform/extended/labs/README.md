# Extended lab code

Each `labNN-*` directory corresponds to a lab manual. Run `terraform init`, `terraform validate`, and `terraform plan` from the lab directory. Examples using AWS require an authenticated profile or IAM role. Backend examples require a pre-created bucket only when you intentionally initialize their S3 backend.
